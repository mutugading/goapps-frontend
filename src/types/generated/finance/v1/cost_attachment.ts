// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               unknown
// source: finance/v1/cost_attachment.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { BaseResponse } from "../../common/v1/common";

export const protobufPackage = "finance.v1";

export interface CostAttachment {
  attachmentId: number;
  /** set when request-level */
  requestId: number;
  /** set when comment-level */
  commentId: number;
  filename: string;
  mimeType: string;
  sizeBytes: number;
  storageKey: string;
  uploadedBy: string;
  uploadedAt: string;
}

export interface UploadCostAttachmentRequest {
  /** Exactly one of (request_id, comment_id) must be > 0. */
  requestId: number;
  commentId: number;
  filename: string;
  mimeType: string;
  /** Max 25 MB per FR-5. */
  fileContent: Uint8Array;
}

export interface UploadCostAttachmentResponse {
  base: BaseResponse | undefined;
  data: CostAttachment | undefined;
}

export interface ListCostAttachmentsByRequestRequest {
  requestId: number;
}

export interface ListCostAttachmentsByRequestResponse {
  base: BaseResponse | undefined;
  data: CostAttachment[];
}

export interface ListCostAttachmentsByCommentRequest {
  commentId: number;
}

export interface ListCostAttachmentsByCommentResponse {
  base: BaseResponse | undefined;
  data: CostAttachment[];
}

export interface GetCostAttachmentDownloadURLRequest {
  attachmentId: number;
}

export interface GetCostAttachmentDownloadURLResponse {
  base: BaseResponse | undefined;
  url: string;
  validSeconds: number;
}

export interface DeleteCostAttachmentRequest {
  attachmentId: number;
}

export interface DeleteCostAttachmentResponse {
  base: BaseResponse | undefined;
}

function createBaseCostAttachment(): CostAttachment {
  return {
    attachmentId: 0,
    requestId: 0,
    commentId: 0,
    filename: "",
    mimeType: "",
    sizeBytes: 0,
    storageKey: "",
    uploadedBy: "",
    uploadedAt: "",
  };
}

export const CostAttachment: MessageFns<CostAttachment> = {
  encode(message: CostAttachment, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.attachmentId !== 0) {
      writer.uint32(8).int64(message.attachmentId);
    }
    if (message.requestId !== 0) {
      writer.uint32(16).int64(message.requestId);
    }
    if (message.commentId !== 0) {
      writer.uint32(24).int64(message.commentId);
    }
    if (message.filename !== "") {
      writer.uint32(34).string(message.filename);
    }
    if (message.mimeType !== "") {
      writer.uint32(42).string(message.mimeType);
    }
    if (message.sizeBytes !== 0) {
      writer.uint32(48).int64(message.sizeBytes);
    }
    if (message.storageKey !== "") {
      writer.uint32(58).string(message.storageKey);
    }
    if (message.uploadedBy !== "") {
      writer.uint32(66).string(message.uploadedBy);
    }
    if (message.uploadedAt !== "") {
      writer.uint32(74).string(message.uploadedAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CostAttachment {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCostAttachment();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.attachmentId = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.requestId = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.commentId = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.filename = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.mimeType = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.sizeBytes = longToNumber(reader.int64());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.storageKey = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.uploadedBy = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.uploadedAt = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CostAttachment {
    return {
      attachmentId: isSet(object.attachmentId)
        ? globalThis.Number(object.attachmentId)
        : isSet(object.attachment_id)
        ? globalThis.Number(object.attachment_id)
        : 0,
      requestId: isSet(object.requestId)
        ? globalThis.Number(object.requestId)
        : isSet(object.request_id)
        ? globalThis.Number(object.request_id)
        : 0,
      commentId: isSet(object.commentId)
        ? globalThis.Number(object.commentId)
        : isSet(object.comment_id)
        ? globalThis.Number(object.comment_id)
        : 0,
      filename: isSet(object.filename) ? globalThis.String(object.filename) : "",
      mimeType: isSet(object.mimeType)
        ? globalThis.String(object.mimeType)
        : isSet(object.mime_type)
        ? globalThis.String(object.mime_type)
        : "",
      sizeBytes: isSet(object.sizeBytes)
        ? globalThis.Number(object.sizeBytes)
        : isSet(object.size_bytes)
        ? globalThis.Number(object.size_bytes)
        : 0,
      storageKey: isSet(object.storageKey)
        ? globalThis.String(object.storageKey)
        : isSet(object.storage_key)
        ? globalThis.String(object.storage_key)
        : "",
      uploadedBy: isSet(object.uploadedBy)
        ? globalThis.String(object.uploadedBy)
        : isSet(object.uploaded_by)
        ? globalThis.String(object.uploaded_by)
        : "",
      uploadedAt: isSet(object.uploadedAt)
        ? globalThis.String(object.uploadedAt)
        : isSet(object.uploaded_at)
        ? globalThis.String(object.uploaded_at)
        : "",
    };
  },

  toJSON(message: CostAttachment): unknown {
    const obj: any = {};
    if (message.attachmentId !== 0) {
      obj.attachmentId = Math.round(message.attachmentId);
    }
    if (message.requestId !== 0) {
      obj.requestId = Math.round(message.requestId);
    }
    if (message.commentId !== 0) {
      obj.commentId = Math.round(message.commentId);
    }
    if (message.filename !== "") {
      obj.filename = message.filename;
    }
    if (message.mimeType !== "") {
      obj.mimeType = message.mimeType;
    }
    if (message.sizeBytes !== 0) {
      obj.sizeBytes = Math.round(message.sizeBytes);
    }
    if (message.storageKey !== "") {
      obj.storageKey = message.storageKey;
    }
    if (message.uploadedBy !== "") {
      obj.uploadedBy = message.uploadedBy;
    }
    if (message.uploadedAt !== "") {
      obj.uploadedAt = message.uploadedAt;
    }
    return obj;
  },

  create(base?: DeepPartial<CostAttachment>): CostAttachment {
    return CostAttachment.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CostAttachment>): CostAttachment {
    const message = createBaseCostAttachment();
    message.attachmentId = object.attachmentId ?? 0;
    message.requestId = object.requestId ?? 0;
    message.commentId = object.commentId ?? 0;
    message.filename = object.filename ?? "";
    message.mimeType = object.mimeType ?? "";
    message.sizeBytes = object.sizeBytes ?? 0;
    message.storageKey = object.storageKey ?? "";
    message.uploadedBy = object.uploadedBy ?? "";
    message.uploadedAt = object.uploadedAt ?? "";
    return message;
  },
};

function createBaseUploadCostAttachmentRequest(): UploadCostAttachmentRequest {
  return { requestId: 0, commentId: 0, filename: "", mimeType: "", fileContent: new Uint8Array(0) };
}

export const UploadCostAttachmentRequest: MessageFns<UploadCostAttachmentRequest> = {
  encode(message: UploadCostAttachmentRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestId !== 0) {
      writer.uint32(8).int64(message.requestId);
    }
    if (message.commentId !== 0) {
      writer.uint32(16).int64(message.commentId);
    }
    if (message.filename !== "") {
      writer.uint32(26).string(message.filename);
    }
    if (message.mimeType !== "") {
      writer.uint32(34).string(message.mimeType);
    }
    if (message.fileContent.length !== 0) {
      writer.uint32(42).bytes(message.fileContent);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UploadCostAttachmentRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUploadCostAttachmentRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.requestId = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.commentId = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.filename = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.mimeType = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.fileContent = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UploadCostAttachmentRequest {
    return {
      requestId: isSet(object.requestId)
        ? globalThis.Number(object.requestId)
        : isSet(object.request_id)
        ? globalThis.Number(object.request_id)
        : 0,
      commentId: isSet(object.commentId)
        ? globalThis.Number(object.commentId)
        : isSet(object.comment_id)
        ? globalThis.Number(object.comment_id)
        : 0,
      filename: isSet(object.filename) ? globalThis.String(object.filename) : "",
      mimeType: isSet(object.mimeType)
        ? globalThis.String(object.mimeType)
        : isSet(object.mime_type)
        ? globalThis.String(object.mime_type)
        : "",
      fileContent: isSet(object.fileContent)
        ? bytesFromBase64(object.fileContent)
        : isSet(object.file_content)
        ? bytesFromBase64(object.file_content)
        : new Uint8Array(0),
    };
  },

  toJSON(message: UploadCostAttachmentRequest): unknown {
    const obj: any = {};
    if (message.requestId !== 0) {
      obj.requestId = Math.round(message.requestId);
    }
    if (message.commentId !== 0) {
      obj.commentId = Math.round(message.commentId);
    }
    if (message.filename !== "") {
      obj.filename = message.filename;
    }
    if (message.mimeType !== "") {
      obj.mimeType = message.mimeType;
    }
    if (message.fileContent.length !== 0) {
      obj.fileContent = base64FromBytes(message.fileContent);
    }
    return obj;
  },

  create(base?: DeepPartial<UploadCostAttachmentRequest>): UploadCostAttachmentRequest {
    return UploadCostAttachmentRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UploadCostAttachmentRequest>): UploadCostAttachmentRequest {
    const message = createBaseUploadCostAttachmentRequest();
    message.requestId = object.requestId ?? 0;
    message.commentId = object.commentId ?? 0;
    message.filename = object.filename ?? "";
    message.mimeType = object.mimeType ?? "";
    message.fileContent = object.fileContent ?? new Uint8Array(0);
    return message;
  },
};

function createBaseUploadCostAttachmentResponse(): UploadCostAttachmentResponse {
  return { base: undefined, data: undefined };
}

export const UploadCostAttachmentResponse: MessageFns<UploadCostAttachmentResponse> = {
  encode(message: UploadCostAttachmentResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.base !== undefined) {
      BaseResponse.encode(message.base, writer.uint32(10).fork()).join();
    }
    if (message.data !== undefined) {
      CostAttachment.encode(message.data, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UploadCostAttachmentResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUploadCostAttachmentResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.base = BaseResponse.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.data = CostAttachment.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UploadCostAttachmentResponse {
    return {
      base: isSet(object.base) ? BaseResponse.fromJSON(object.base) : undefined,
      data: isSet(object.data) ? CostAttachment.fromJSON(object.data) : undefined,
    };
  },

  toJSON(message: UploadCostAttachmentResponse): unknown {
    const obj: any = {};
    if (message.base !== undefined) {
      obj.base = BaseResponse.toJSON(message.base);
    }
    if (message.data !== undefined) {
      obj.data = CostAttachment.toJSON(message.data);
    }
    return obj;
  },

  create(base?: DeepPartial<UploadCostAttachmentResponse>): UploadCostAttachmentResponse {
    return UploadCostAttachmentResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UploadCostAttachmentResponse>): UploadCostAttachmentResponse {
    const message = createBaseUploadCostAttachmentResponse();
    message.base = (object.base !== undefined && object.base !== null)
      ? BaseResponse.fromPartial(object.base)
      : undefined;
    message.data = (object.data !== undefined && object.data !== null)
      ? CostAttachment.fromPartial(object.data)
      : undefined;
    return message;
  },
};

function createBaseListCostAttachmentsByRequestRequest(): ListCostAttachmentsByRequestRequest {
  return { requestId: 0 };
}

export const ListCostAttachmentsByRequestRequest: MessageFns<ListCostAttachmentsByRequestRequest> = {
  encode(message: ListCostAttachmentsByRequestRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestId !== 0) {
      writer.uint32(8).int64(message.requestId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListCostAttachmentsByRequestRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListCostAttachmentsByRequestRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.requestId = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListCostAttachmentsByRequestRequest {
    return {
      requestId: isSet(object.requestId)
        ? globalThis.Number(object.requestId)
        : isSet(object.request_id)
        ? globalThis.Number(object.request_id)
        : 0,
    };
  },

  toJSON(message: ListCostAttachmentsByRequestRequest): unknown {
    const obj: any = {};
    if (message.requestId !== 0) {
      obj.requestId = Math.round(message.requestId);
    }
    return obj;
  },

  create(base?: DeepPartial<ListCostAttachmentsByRequestRequest>): ListCostAttachmentsByRequestRequest {
    return ListCostAttachmentsByRequestRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListCostAttachmentsByRequestRequest>): ListCostAttachmentsByRequestRequest {
    const message = createBaseListCostAttachmentsByRequestRequest();
    message.requestId = object.requestId ?? 0;
    return message;
  },
};

function createBaseListCostAttachmentsByRequestResponse(): ListCostAttachmentsByRequestResponse {
  return { base: undefined, data: [] };
}

export const ListCostAttachmentsByRequestResponse: MessageFns<ListCostAttachmentsByRequestResponse> = {
  encode(message: ListCostAttachmentsByRequestResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.base !== undefined) {
      BaseResponse.encode(message.base, writer.uint32(10).fork()).join();
    }
    for (const v of message.data) {
      CostAttachment.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListCostAttachmentsByRequestResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListCostAttachmentsByRequestResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.base = BaseResponse.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.data.push(CostAttachment.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListCostAttachmentsByRequestResponse {
    return {
      base: isSet(object.base) ? BaseResponse.fromJSON(object.base) : undefined,
      data: globalThis.Array.isArray(object?.data) ? object.data.map((e: any) => CostAttachment.fromJSON(e)) : [],
    };
  },

  toJSON(message: ListCostAttachmentsByRequestResponse): unknown {
    const obj: any = {};
    if (message.base !== undefined) {
      obj.base = BaseResponse.toJSON(message.base);
    }
    if (message.data?.length) {
      obj.data = message.data.map((e) => CostAttachment.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ListCostAttachmentsByRequestResponse>): ListCostAttachmentsByRequestResponse {
    return ListCostAttachmentsByRequestResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListCostAttachmentsByRequestResponse>): ListCostAttachmentsByRequestResponse {
    const message = createBaseListCostAttachmentsByRequestResponse();
    message.base = (object.base !== undefined && object.base !== null)
      ? BaseResponse.fromPartial(object.base)
      : undefined;
    message.data = object.data?.map((e) => CostAttachment.fromPartial(e)) || [];
    return message;
  },
};

function createBaseListCostAttachmentsByCommentRequest(): ListCostAttachmentsByCommentRequest {
  return { commentId: 0 };
}

export const ListCostAttachmentsByCommentRequest: MessageFns<ListCostAttachmentsByCommentRequest> = {
  encode(message: ListCostAttachmentsByCommentRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.commentId !== 0) {
      writer.uint32(8).int64(message.commentId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListCostAttachmentsByCommentRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListCostAttachmentsByCommentRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.commentId = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListCostAttachmentsByCommentRequest {
    return {
      commentId: isSet(object.commentId)
        ? globalThis.Number(object.commentId)
        : isSet(object.comment_id)
        ? globalThis.Number(object.comment_id)
        : 0,
    };
  },

  toJSON(message: ListCostAttachmentsByCommentRequest): unknown {
    const obj: any = {};
    if (message.commentId !== 0) {
      obj.commentId = Math.round(message.commentId);
    }
    return obj;
  },

  create(base?: DeepPartial<ListCostAttachmentsByCommentRequest>): ListCostAttachmentsByCommentRequest {
    return ListCostAttachmentsByCommentRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListCostAttachmentsByCommentRequest>): ListCostAttachmentsByCommentRequest {
    const message = createBaseListCostAttachmentsByCommentRequest();
    message.commentId = object.commentId ?? 0;
    return message;
  },
};

function createBaseListCostAttachmentsByCommentResponse(): ListCostAttachmentsByCommentResponse {
  return { base: undefined, data: [] };
}

export const ListCostAttachmentsByCommentResponse: MessageFns<ListCostAttachmentsByCommentResponse> = {
  encode(message: ListCostAttachmentsByCommentResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.base !== undefined) {
      BaseResponse.encode(message.base, writer.uint32(10).fork()).join();
    }
    for (const v of message.data) {
      CostAttachment.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListCostAttachmentsByCommentResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListCostAttachmentsByCommentResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.base = BaseResponse.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.data.push(CostAttachment.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListCostAttachmentsByCommentResponse {
    return {
      base: isSet(object.base) ? BaseResponse.fromJSON(object.base) : undefined,
      data: globalThis.Array.isArray(object?.data) ? object.data.map((e: any) => CostAttachment.fromJSON(e)) : [],
    };
  },

  toJSON(message: ListCostAttachmentsByCommentResponse): unknown {
    const obj: any = {};
    if (message.base !== undefined) {
      obj.base = BaseResponse.toJSON(message.base);
    }
    if (message.data?.length) {
      obj.data = message.data.map((e) => CostAttachment.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ListCostAttachmentsByCommentResponse>): ListCostAttachmentsByCommentResponse {
    return ListCostAttachmentsByCommentResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListCostAttachmentsByCommentResponse>): ListCostAttachmentsByCommentResponse {
    const message = createBaseListCostAttachmentsByCommentResponse();
    message.base = (object.base !== undefined && object.base !== null)
      ? BaseResponse.fromPartial(object.base)
      : undefined;
    message.data = object.data?.map((e) => CostAttachment.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGetCostAttachmentDownloadURLRequest(): GetCostAttachmentDownloadURLRequest {
  return { attachmentId: 0 };
}

export const GetCostAttachmentDownloadURLRequest: MessageFns<GetCostAttachmentDownloadURLRequest> = {
  encode(message: GetCostAttachmentDownloadURLRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.attachmentId !== 0) {
      writer.uint32(8).int64(message.attachmentId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetCostAttachmentDownloadURLRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetCostAttachmentDownloadURLRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.attachmentId = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetCostAttachmentDownloadURLRequest {
    return {
      attachmentId: isSet(object.attachmentId)
        ? globalThis.Number(object.attachmentId)
        : isSet(object.attachment_id)
        ? globalThis.Number(object.attachment_id)
        : 0,
    };
  },

  toJSON(message: GetCostAttachmentDownloadURLRequest): unknown {
    const obj: any = {};
    if (message.attachmentId !== 0) {
      obj.attachmentId = Math.round(message.attachmentId);
    }
    return obj;
  },

  create(base?: DeepPartial<GetCostAttachmentDownloadURLRequest>): GetCostAttachmentDownloadURLRequest {
    return GetCostAttachmentDownloadURLRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetCostAttachmentDownloadURLRequest>): GetCostAttachmentDownloadURLRequest {
    const message = createBaseGetCostAttachmentDownloadURLRequest();
    message.attachmentId = object.attachmentId ?? 0;
    return message;
  },
};

function createBaseGetCostAttachmentDownloadURLResponse(): GetCostAttachmentDownloadURLResponse {
  return { base: undefined, url: "", validSeconds: 0 };
}

export const GetCostAttachmentDownloadURLResponse: MessageFns<GetCostAttachmentDownloadURLResponse> = {
  encode(message: GetCostAttachmentDownloadURLResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.base !== undefined) {
      BaseResponse.encode(message.base, writer.uint32(10).fork()).join();
    }
    if (message.url !== "") {
      writer.uint32(18).string(message.url);
    }
    if (message.validSeconds !== 0) {
      writer.uint32(24).int32(message.validSeconds);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetCostAttachmentDownloadURLResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetCostAttachmentDownloadURLResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.base = BaseResponse.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.url = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.validSeconds = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetCostAttachmentDownloadURLResponse {
    return {
      base: isSet(object.base) ? BaseResponse.fromJSON(object.base) : undefined,
      url: isSet(object.url) ? globalThis.String(object.url) : "",
      validSeconds: isSet(object.validSeconds)
        ? globalThis.Number(object.validSeconds)
        : isSet(object.valid_seconds)
        ? globalThis.Number(object.valid_seconds)
        : 0,
    };
  },

  toJSON(message: GetCostAttachmentDownloadURLResponse): unknown {
    const obj: any = {};
    if (message.base !== undefined) {
      obj.base = BaseResponse.toJSON(message.base);
    }
    if (message.url !== "") {
      obj.url = message.url;
    }
    if (message.validSeconds !== 0) {
      obj.validSeconds = Math.round(message.validSeconds);
    }
    return obj;
  },

  create(base?: DeepPartial<GetCostAttachmentDownloadURLResponse>): GetCostAttachmentDownloadURLResponse {
    return GetCostAttachmentDownloadURLResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetCostAttachmentDownloadURLResponse>): GetCostAttachmentDownloadURLResponse {
    const message = createBaseGetCostAttachmentDownloadURLResponse();
    message.base = (object.base !== undefined && object.base !== null)
      ? BaseResponse.fromPartial(object.base)
      : undefined;
    message.url = object.url ?? "";
    message.validSeconds = object.validSeconds ?? 0;
    return message;
  },
};

function createBaseDeleteCostAttachmentRequest(): DeleteCostAttachmentRequest {
  return { attachmentId: 0 };
}

export const DeleteCostAttachmentRequest: MessageFns<DeleteCostAttachmentRequest> = {
  encode(message: DeleteCostAttachmentRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.attachmentId !== 0) {
      writer.uint32(8).int64(message.attachmentId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteCostAttachmentRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteCostAttachmentRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.attachmentId = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteCostAttachmentRequest {
    return {
      attachmentId: isSet(object.attachmentId)
        ? globalThis.Number(object.attachmentId)
        : isSet(object.attachment_id)
        ? globalThis.Number(object.attachment_id)
        : 0,
    };
  },

  toJSON(message: DeleteCostAttachmentRequest): unknown {
    const obj: any = {};
    if (message.attachmentId !== 0) {
      obj.attachmentId = Math.round(message.attachmentId);
    }
    return obj;
  },

  create(base?: DeepPartial<DeleteCostAttachmentRequest>): DeleteCostAttachmentRequest {
    return DeleteCostAttachmentRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DeleteCostAttachmentRequest>): DeleteCostAttachmentRequest {
    const message = createBaseDeleteCostAttachmentRequest();
    message.attachmentId = object.attachmentId ?? 0;
    return message;
  },
};

function createBaseDeleteCostAttachmentResponse(): DeleteCostAttachmentResponse {
  return { base: undefined };
}

export const DeleteCostAttachmentResponse: MessageFns<DeleteCostAttachmentResponse> = {
  encode(message: DeleteCostAttachmentResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.base !== undefined) {
      BaseResponse.encode(message.base, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteCostAttachmentResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteCostAttachmentResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.base = BaseResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteCostAttachmentResponse {
    return { base: isSet(object.base) ? BaseResponse.fromJSON(object.base) : undefined };
  },

  toJSON(message: DeleteCostAttachmentResponse): unknown {
    const obj: any = {};
    if (message.base !== undefined) {
      obj.base = BaseResponse.toJSON(message.base);
    }
    return obj;
  },

  create(base?: DeepPartial<DeleteCostAttachmentResponse>): DeleteCostAttachmentResponse {
    return DeleteCostAttachmentResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DeleteCostAttachmentResponse>): DeleteCostAttachmentResponse {
    const message = createBaseDeleteCostAttachmentResponse();
    message.base = (object.base !== undefined && object.base !== null)
      ? BaseResponse.fromPartial(object.base)
      : undefined;
    return message;
  },
};

export type CostAttachmentServiceDefinition = typeof CostAttachmentServiceDefinition;
export const CostAttachmentServiceDefinition = {
  name: "CostAttachmentService",
  fullName: "finance.v1.CostAttachmentService",
  methods: {
    uploadCostAttachment: {
      name: "UploadCostAttachment",
      requestType: UploadCostAttachmentRequest,
      requestStream: false,
      responseType: UploadCostAttachmentResponse,
      responseStream: false,
      options: {},
    },
    listCostAttachmentsByRequest: {
      name: "ListCostAttachmentsByRequest",
      requestType: ListCostAttachmentsByRequestRequest,
      requestStream: false,
      responseType: ListCostAttachmentsByRequestResponse,
      responseStream: false,
      options: {},
    },
    listCostAttachmentsByComment: {
      name: "ListCostAttachmentsByComment",
      requestType: ListCostAttachmentsByCommentRequest,
      requestStream: false,
      responseType: ListCostAttachmentsByCommentResponse,
      responseStream: false,
      options: {},
    },
    getCostAttachmentDownloadURL: {
      name: "GetCostAttachmentDownloadURL",
      requestType: GetCostAttachmentDownloadURLRequest,
      requestStream: false,
      responseType: GetCostAttachmentDownloadURLResponse,
      responseStream: false,
      options: {},
    },
    deleteCostAttachment: {
      name: "DeleteCostAttachment",
      requestType: DeleteCostAttachmentRequest,
      requestStream: false,
      responseType: DeleteCostAttachmentResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
